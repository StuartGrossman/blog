import React from 'react';
import MessageBoard from './MessageBoard';

const Home: React.FC = () => {
    return (
        <div className="min-h-screen bg-gray-100">
            <MessageBoard />
        </div>
    );
};

export default Home; 